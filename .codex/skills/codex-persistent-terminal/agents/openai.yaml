interface:
  display_name: "Codex Persistent Terminal"
  short_description: "Run Codex in tmux and resume the same conversation."
  default_prompt: "Match the requested persistent-terminal operation. Inspect an existing named tmux session read-only, create only on an explicit start request, and recover with an exact SESSION_ID when available without guessing among ambiguous sessions."
