interface:
  display_name: "Artifact Manager"
  short_description: "Manage one latest artifact with temporary rollback history."
  default_prompt: "Maintain one canonical latest generated artifact and short-lived rollback history under WORKSPACE/.codex/tmp/artifacts. Keep durable evidence in a project-owned location and never delete ambiguous files."
