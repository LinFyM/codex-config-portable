interface:
  display_name: "GPU Preflight"
  short_description: "Verify live GPU identity, load, and ownership"
  default_prompt: "Use $gpu-preflight as one focused live scheduling snapshot; report device identity or blockers without creating a second workflow or historical health claim."

policy:
  allow_implicit_invocation: true
