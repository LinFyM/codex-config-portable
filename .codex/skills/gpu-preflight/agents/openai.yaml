interface:
  display_name: "GPU Preflight"
  short_description: "Check gpu01/gpu02 GPUs and recommend idle devices."
  default_prompt: "Check both gpu01 and gpu02 GPU utilization/memory and recommend the best GPUs to run a new job. Include a concise per-node summary and a recommendation."
