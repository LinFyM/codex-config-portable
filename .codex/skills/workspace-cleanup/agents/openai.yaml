interface:
  display_name: "Workspace Cleanup"
  short_description: "Retire obsolete code, docs, scripts, and artifacts"
  default_prompt: "Use $workspace-cleanup to audit the whole repository and workspace, retain one canonical active path, and remove or consolidate only verified obsolete code, tests, scripts, configs, docs, and artifacts."
